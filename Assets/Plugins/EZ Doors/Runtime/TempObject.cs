using UnityEngine;

namespace EZDoor
{
    [SelectionBase]
    public class TempObject : MonoBehaviour
    {
        // Empty class just so we can select the root level temp object when creating a custom door
    }
}
